List of Linux Commands 
pwd = checks current directory you are in. 
mkdir = to create a directory.
cd = to change the current directory to another directory.
ls = to list the files inside a directory. 
ls -ltr = lists one file per line and sorts it by modification time.
cat = to print the file to standard output.

List of Git Commands
git config = used to set Git configuration values on a global or local project.
git clone = copies a repo directory into a new directory.
git status = to check the status of your local repository.
git add = adds a file or a group of files to a list of files to be updated in the next commit.
git commit = to commit the changes and include a message describing the changes. 
git push = pushes changes in the local repository to the github repository.
